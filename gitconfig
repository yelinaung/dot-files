[user]
    name  = Ye Lin Aung
    email = me@yelinaung.com
[github]
    user = yelinaung
[hub]
    protocol = https
[core]
    ; pager = diff-so-fancy | less --tabs=4 -RFX
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	editor = nvim
    autocrlf = false
    safecrlf = false
    excludesfile = /home/yelin/.gitignore_global
[advice]
    statusHints = false
[web]
    browser = open
[push]
    config = default
    default = tracking
[rebase]
    autosquash = true
[merge]
    summary = true
	ff = false
[alias]
    hist = log --graph --pretty=format:'%C(magenta)%h%Creset -%C(red)%d%Creset %s %C(dim green)(%cr) %C(cyan)<%an>%Creset' --abbrev-commit --date=short
    reset-one = reset HEAD~1

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    plus-style = "syntax #012800"
    minus-style = "syntax #340001"
    syntax-theme = Monokai Extended
    navigate = true
    features = side-by-side line-numbers decorations
    whitespace-error-style = 22 reverse

[delta "decorations"]
    commit-decoration-style = bold yellow box ul
    file-style = bold yellow ul
    file-decoration-style = none

[interactive]
    diffFilter = delta --color-only
[color]
    status = auto
    branch = auto
    interactive = auto
    ui = true
    pager = true
[color "branch"]
    current = yellow reverse
    local   = yellow
    remote  = green
[help]
	autocorrect = 1
    meta = blue
    frag = magenta
    old  = red
    new  = green
[color "status"]
    added = yellow
    changed = green
    untracked = blue
