[user]
        name  = Ye Lin Aung
        email = me@yelinaung.com
[github]
        user = yelinaung
[hub]
        protocol = https
[core]
        pager = less -FRSX
        whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
        editor = nvim
        autocrlf = false
        safecrlf = false
        excludesfile = /home/yelin/.gitignore_global
[advice]
        statusHints = false
[web]
        browser = open
[push]
        config = default
        default = tracking
[rebase]
        autosquash = true
[merge]
        summary = true
[alias]
        hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
[diff]
        tool = icdiff
[difftool]
        prompt = false
[difftool "icdiff"]
        cmd = /home/yelinaung/work/tools/icdiff/icdiff --line-numbers $LOCAL $REMOTE
[color]
        diff = auto
        status = auto
        branch = auto
        interactive = auto
        ui = true
        pager = true
[color "branch"]
        current = yellow reverse
        local   = yellow
        remote  = green
[color "diff"]
        meta = blue
        frag = magenta
        old  = red
        new  = green
[color "status"]
        added = yellow
        changed = green
        untracked = blue
